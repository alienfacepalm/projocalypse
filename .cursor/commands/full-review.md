# Full review and ship

Run the **`projocalypse-full-review`** skill end-to-end on the current branch.

@.cursor/skills/projocalypse-full-review/SKILL.md

## Do now

1. Confirm branch (not `master`/`main`); read README + matching rules for this diff.
2. Plan + code gap review (data model, export/import, views, docs, tests).
3. Fix errors; add missing tests; run `pnpm test:pr` or scoped tests from the skill table.
4. If deliverable and tests pass: commit on the feature branch, push, open/update PR (unless the user said WIP / no PR).

## Report

- Plan alignment (matched / gaps fixed)
- Fixes and tests run
- PR URL or push link if shipped
